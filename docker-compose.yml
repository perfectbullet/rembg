# You can set variables in .env file in root folder
#
# PUBLIC_PORT=7000:7000
# REPLICAS_COUNT=1

version: '3'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile_nvidia_cuda_cudnn_gpu
    command: ["s"]
    deploy:
      replicas: ${REPLICAS_COUNT:-1}
    ports:
      - ${PUBLIC_PORT:-7000:7000}
